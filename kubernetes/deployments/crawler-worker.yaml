apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-worker
  namespace: veggiescore
  labels:
    app: crawler-worker
    component: worker
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: crawler-worker
  template:
    metadata:
      labels:
        app: crawler-worker
        component: worker
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      containers:
      - name: worker
        image: veggiescore/crawler-worker:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        env:
        - name: REDIS_HOST
          value: "redis-master"
        - name: REDIS_PORT
          value: "6379"
        - name: SUPABASE_URL
          valueFrom:
            secretKeyRef:
              name: veggiescore-secrets
              key: supabase-url
        - name: SUPABASE_SERVICE_KEY
          valueFrom:
            secretKeyRef:
              name: veggiescore-secrets
              key: supabase-service-key
        - name: WORKER_CONCURRENCY
          value: "3"
        - name: SENTRY_DSN
          valueFrom:
            secretKeyRef:
              name: veggiescore-secrets
              key: sentry-dsn
              optional: true
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: crawler-worker
  namespace: veggiescore
spec:
  selector:
    app: crawler-worker
  ports:
  - name: metrics
    port: 9090
  clusterIP: None
